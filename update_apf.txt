The Audio Processing Framework (APF)

prerequisites: git subtree (included in git >= 1.7.11, but only in "contrib")

git remote add -f apf https://username@dev.qu.tu-berlin.de/git/apf-internal

# only first time:
git subtree add --prefix=apf --squash apf/master

git subtree pull --prefix=apf --squash apf/master

# maybe an alternative:
git remote update
git subtree merge ...?

git subtree split --prefix=apf --annotate='(SSR) ' --branch ssr-changes

# untested:

git push https://username@dev.qu.tu-berlin.de/git/apf-internal ssr-changes:master

# maybe better without the local data?

git subtree add --prefix=apf --squash https://username@dev.qu.tu-berlin.de/git/apf-internal master

git subtree pull --prefix=apf --squash https://username@dev.qu.tu-berlin.de/git/apf-internal master
